from bst import TreeNode
from bst import insert
from bst import inorder
from bst_height import height


if __name__ == "__main__":

    root = TreeNode(10)
    insert(root, TreeNode(5))
    insert(root, TreeNode(7))
    insert(root, TreeNode(15))
    insert(root, TreeNode(25))
    insert(root, TreeNode(9))
    insert(root, TreeNode(10))
    insert(root, TreeNode(21))
    insert(root, TreeNode(35))
    insert(root, TreeNode(13))
    insert(root, TreeNode(4))
    insert(root, TreeNode(1))

    # inorder(root)
    # print height(root)
    #





